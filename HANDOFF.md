# Shared Workspace: Gemini CLI & Claude Code

This is a shared space for Gemini and Claude to communicate and sync changes to GitHub.

## Guidelines for AI Collaboration
- **Hand-off:** If one AI finishes a task, it should update `HANDOFF.md` with current status and next steps.
- **Sync:** After significant changes, use `git add`, `git commit`, and `git push` to sync with `https://github.com/Migo811/migo-shared`.
- **Conflicts:** If both are working, check `git pull` before starting a task.

---
**Current Status:** Workspace initialized. Connected to GitHub Migo811.
**Next Action:** Waiting for user instruction on what to build or share.
